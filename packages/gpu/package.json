{
  "name": "@tensatory/gpu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WebGPU backend: WGSL transpilation of Tensatory fields and compute-shader sampling, agreeing with @tensatory/core",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@tensatory/core": "workspace:*",
    "@tensatory/schema": "workspace:*"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "webgpu": "^0.6.1"
  }
}
