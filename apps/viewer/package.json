{
  "name": "@tensatory/viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@tensatory/core": "workspace:*",
    "@tensatory/schema": "workspace:*",
    "@tensatory/gpu": "workspace:*"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "@webgpu/types": "^0.1.60"
  }
}
